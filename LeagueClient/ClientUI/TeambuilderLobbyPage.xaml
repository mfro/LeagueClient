<Page x:Class="LeagueClient.ClientUI.TeambuilderLobbyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:my="clr-namespace:LeagueClient.ClientUI.Controls"
      SnapsToDevicePixels="True">
  <Grid Width="{StaticResource Width}" Height="{StaticResource PageHeight}">
    <Grid.RowDefinitions>
      <RowDefinition Height="75"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="75"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="75"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="auto"/>
      <ColumnDefinition Width="75"/>
    </Grid.ColumnDefinitions>

    <Button Grid.Column="2" Width="80" Height="20" Click="Invite_Click">Invite</Button>

    <Border VerticalAlignment="Top" Grid.Column="2" Grid.Row="1"
            BorderBrush="{StaticResource Back1Color}" BorderThickness="4" Height="310">
      <my:TeambuilderMap x:Name="GameMap"/>
    </Border>
  </Grid>
</Page>
