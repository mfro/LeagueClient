<UserControl x:Class="LeagueClient.ClientUI.Controls.Friend"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             MouseEnter="UserControl_MouseEnter" MouseLeave="UserControl_MouseLeave"
             MouseDown="UserControl_MouseDown" MouseUp="UserControl_MouseUp"
             Background="{StaticResource Back1Color}" Cursor="Hand" BorderBrush="White">
  <Grid Width="240" Height="48">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="48"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Image Name="ProfileIcon" Grid.Column="0" Margin="4" Grid.RowSpan="3" RenderOptions.BitmapScalingMode="HighQuality"/>
    <TextBlock Style="{StaticResource Control}" Name="NameText" Grid.Column="1" Grid.Row="0" VerticalAlignment="Bottom"/>
    <TextBlock Style="{StaticResource Control}" Name="MsgText" FontSize="11" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center"/>
    <TextBlock Style="{StaticResource Control}" Name="InGameText" FontSize="11" Grid.Column="1" Grid.Row="2" VerticalAlignment="Top"/>
  </Grid>
</UserControl>
