<Page x:Class="LeagueClient.ClientUI.Main.PlaySelectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">
  <Page.Resources>
    <Style TargetType="ListBoxItem" x:Key="ItemStyle">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border Name="_border" Padding="3 2">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="_border" Property="Background" Value="{StaticResource FocusBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ListBox">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Background" Value="{StaticResource Back1Brush}"/>
      <Setter Property="ItemContainerStyle" Value="{StaticResource ItemStyle}"/>
      <Setter Property="ItemTemplate">
        <Setter.Value>
          <DataTemplate>
            <TextBlock Style="{StaticResource Control}" FontSize="14" Height="20" Text="{Binding Name}"/>
          </DataTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
              <StackPanel Orientation="Vertical" IsItemsHost="True"/>
            </ScrollViewer>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Page.Resources>
  <Grid Width="{StaticResource Width}" Height="{StaticResource PageHeight}" Background="{StaticResource Back2Brush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="40"/>
      <RowDefinition Height="190"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="40"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="150"/>
      <ColumnDefinition Width="150"/>
      <ColumnDefinition Width="150"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <UserControl Background="{StaticResource Back1Brush}" Margin="-5" Grid.Row="1" Grid.RowSpan="2"
                 Grid.Column="1" Grid.ColumnSpan="3"/>
    <Image Grid.Row="1" Grid.RowSpan="2" Name="BackImage1"/>
    <Image Grid.Row="1" Grid.RowSpan="2" Name="BackImage2" Grid.Column="4"/>
    <!--Region Lists-->
    <ListBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding GameGroups}"
             Name="GroupList" Loaded="ListBox_Loaded">
    </ListBox>
    <ListBox Grid.Column="2" Grid.Row="1" ItemsSource="{Binding GameModes}"
             Name="ModeList" Loaded="ListBox_Loaded">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="18"/>
              <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Style="{StaticResource Control}" FontSize="14" Text="{Binding Name}"/>
            <TextBlock Style="{StaticResource Control}" Grid.Row="1" FontSize="12" Text="{Binding Info}"/>
          </Grid>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <ListBox Grid.Column="3" Grid.Row="1" ItemsSource="{Binding GameQueues}"
             Name="QueueList" Loaded="ListBox_Loaded">
    </ListBox>
    <!--End Region-->
    <Grid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Background="{StaticResource Back1Brush}">
      <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>
      <TextBlock Style="{StaticResource Control}" TextWrapping="Wrap" Padding="4" FontSize="16"  Name="ModeTitle"
                 Background="{StaticResource ForeBrush}" Visibility="Collapsed"/>
      <TextBlock Style="{StaticResource Control}" TextWrapping="Wrap" Padding="4" FontSize="16" HorizontalAlignment="Right" Name="QueueTitle"/>
      <TextBlock Style="{StaticResource Control}" FontSize="14" TextWrapping="Wrap" Grid.Row="1" Padding="4" Name="GroupDetails"/>
      <TextBlock Style="{StaticResource Control}" FontSize="14" TextWrapping="Wrap" Grid.Row="2" Padding="4" Name="ModeDetails"/>
      <Button HorizontalAlignment="Right" VerticalAlignment="Bottom"
              Margin="0 0 155 0" Width="150" Grid.Row="3" Name="Join1"
              Visibility="Collapsed" Click="Join1_Click"/>
      <Button HorizontalAlignment="Right" VerticalAlignment="Bottom"
              Margin="0" Width="150" Grid.Row="3" Name="Join2"
              Visibility="Collapsed" Click="Join2_Click"/>
    </Grid>
  </Grid>
</Page>
