<UserControl x:Class="LeagueClient.ClientUI.Main.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:my="clr-namespace:LeagueClient.ClientUI.Controls"
             Width="{StaticResource PageWidth}" Height="{StaticResource PageHeight}">
  <Grid>
    <Grid Name="NewsGrid">
      <ScrollViewer>
        <ItemsControl Name="NewsList" Background="Transparent" BorderThickness="0" Margin="0 0 10 -10">
          <ItemsControl.Resources>
            <Style TargetType="my:NewsItem">
              <Setter Property="Margin" Value="0 0 0 10"/>
            </Style>
          </ItemsControl.Resources>
        </ItemsControl>
      </ScrollViewer>
    </Grid>

  </Grid>
</UserControl>
